---
const {class: className, id } = Astro.props
---

<section  id = {id}  class={`section ${className} scroll-m-20 w-full mx-auto container lg:max-w-5xl md:max-w-2xl justify-center `}>
    <slot/>
</section>